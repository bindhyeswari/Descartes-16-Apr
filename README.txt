Study on HTTP Verbs - http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html

npm install mongoose --save
Launch the mongodb server by running the command $ mongod or $ mongod --dbpath <path to databse> or $ sudo mongod
mongoose.connect('mongodb://localhost/test');

Homework:
1. Change the model so that it validates username for characters (a-zA-Z only) and email  for a regular expression.



